# Entra-App-Registration-Expiration-Report
Report for Entra ID (Azure AD) app registrations on expiring or expired client secrets and certificates. Uses app-only authentication with a certificate thumbprint.
